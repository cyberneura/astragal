{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "small"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:default"
  ]
}
